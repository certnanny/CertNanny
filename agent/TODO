
Current limitations and roadmap

Keystores:

* create PKCS#12 keystore backend (Oracle)
* Lotus Notes Keyring

Platforms:

* support z/OS via USS and RACF


SCEP servers:

* support OpenXPKI


***
* don't renew based on expired certifiates
* CertNanny creates warnings when parsing certs with large serial numbers
* OpenSSL keystore: new option cacertbundle: root certs + intermediate certs
